# Project Rules

- **Git Synchronization**: Whenever code changes, bug fixes, or new features are completed in this repository, stage, commit with a descriptive message, and push the changes to GitHub (`git push origin main`).
